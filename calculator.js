const calculator = {
    Add: (a, b) => { return a + b; },
    Substract: (a, b) => { return a - b; },
    Divide: (a, b) => { return a / b; },
    Multiply: (a, b) => { return a * b; }
    
};

export default calculator;